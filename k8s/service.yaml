apiVersion: v1
kind: Service
metadata:
  name: quote-service
  namespace: harness-mcp-demo
  labels:
    app: quote-service
spec:
  type: ClusterIP
  selector:
    app: quote-service
  ports:
    - name: http
      port: 80
      targetPort: 3000
